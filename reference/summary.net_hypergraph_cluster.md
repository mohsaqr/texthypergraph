# Summary method for net_hypergraph_cluster

Summary method for net_hypergraph_cluster

## Usage

``` r
# S3 method for class 'net_hypergraph_cluster'
summary(object, ...)
```

## Arguments

- object:

  A `net_hypergraph_cluster` object.

- ...:

  Additional arguments (ignored).

## Value

A data.frame, one row per cluster: `cluster`, `size`, `share`.
