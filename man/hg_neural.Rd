% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neural.R
\name{hg_neural}
\alias{hg_neural}
\title{Hypergraph neural network classifier (HGNN)}
\usage{
hg_neural(
  hg,
  labels,
  features = "incidence",
  hidden = 128L,
  epochs = 600L,
  lr = 0.01,
  weight_decay = 5e-04,
  dropout = 0.5,
  validation = 0.1,
  edge_weights = NULL,
  seed = 1L,
  verbose = FALSE
)
}
\arguments{
\item{hg}{A \code{\link[=text_hypergraph]{text_hypergraph()}} (or any Nestimate \code{net_hypergraph}),
dense or sparse.}

\item{labels}{Named character vector: names are node identifiers,
values their known class labels. At least two classes.}

\item{features}{Vertex feature matrix, one row per node in \code{hg} node
order (rownames must match the node names), e.g. sbert embeddings.
The default \code{"incidence"} uses the hypergraph's own weighted
incidence rows (tf-idf bag-of-words features for a \code{nodes = "doc"}
text hypergraph).}

\item{hidden}{Width of the hidden layer.}

\item{epochs}{Training epochs (Adam).}

\item{lr, weight_decay}{Adam learning rate and L2 penalty. The defaults
(\code{lr = 0.01}, \code{epochs = 600}) were selected by validation accuracy on
held-out seeds for high-dimensional sparse text features; the original
paper's \code{lr = 0.001}, 200 epochs underfits that regime.}

\item{dropout}{Dropout rate after the hidden layer.}

\item{validation}{Fraction of the labeled seeds held out (stratified)
to pick the best epoch; \code{0} trains on all seeds for \code{epochs} and
keeps the final weights.}

\item{edge_weights}{Optional positive hyperedge weights \code{W} (one per
hyperedge); default all 1, the paper's initialization.}

\item{seed}{Integer seed for weight initialization, dropout and the
validation split (torch and R RNGs); results are deterministic
given a seed.}

\item{verbose}{If \code{TRUE}, message the loss every 20 epochs.}
}
\value{
A base \code{data.frame}, one row per node, with columns \code{node},
\code{label} (the given label or \code{NA}), \code{predicted}, \code{score} (softmax
probability of the winning class) and \code{margin} (winner minus
runner-up probability). The training history is attached as
attribute \code{"history"} (data.frame: \code{epoch}, \code{loss},
\code{val_accuracy}).
}
\description{
Trains the two-layer hypergraph convolutional network of Feng et al.
(2019) on a hypergraph: each layer propagates linearly transformed
vertex features through \eqn{G = D_v^{-1/2} H W D_e^{-1} H^T
D_v^{-1/2}} (the Zhou similarity operator), with ReLU and dropout
between layers and a cross-entropy loss on the labeled vertices.
Needs the suggested \pkg{torch} package.
}
\examples{
\donttest{
if (requireNamespace("torch", quietly = TRUE)) {
  hg <- text_hypergraph(c(
    cooking_1 = "simmer the soup with onions and carrots",
    cooking_2 = "this soup recipe needs salt on a cold night",
    space_1 = "the telescope revealed a distant galaxy and stars",
    space_2 = "astronomers aimed the telescope at the stars all night"
  ), stop_words = c("the", "with", "and", "a", "this", "at", "on", "all"))
  hg_neural(hg, labels = c(cooking_1 = "cooking", space_1 = "space"),
            hidden = 8, epochs = 50, validation = 0)
}
}
}
\references{
Feng, Y., You, H., Zhang, Z., Ji, R., & Gao, Y. (2019). Hypergraph
neural networks. \emph{AAAI 33}.
}
