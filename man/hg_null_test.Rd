% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null_test.R
\name{hg_null_test}
\alias{hg_null_test}
\title{Degree-preserving null-model test of hypergraph structure}
\usage{
hg_null_test(
  hg,
  statistic = c("pairwise_participation", "density", "avg_edge_size", "avg_jaccard"),
  n = 199L,
  seed = NULL,
  alternative = c("two_sided", "greater", "less")
)
}
\arguments{
\item{hg}{A \code{\link[=text_hypergraph]{text_hypergraph()}}, \code{\link[=knn_hypergraph]{knn_hypergraph()}}, or any Nestimate
\code{net_hypergraph}.}

\item{statistic}{Statistics to test; any of \code{"pairwise_participation"},
\code{"density"}, \code{"avg_edge_size"}, \code{"avg_jaccard"} (mean pairwise edge
Jaccard). Several allowed.}

\item{n}{Number of null samples (default \code{199L}).}

\item{seed}{Seed for the swap chain; set it for a reproducible test.
The global RNG state is restored on exit.}

\item{alternative}{\code{"two_sided"} (default), \code{"greater"}, or \code{"less"}.}
}
\value{
A base \code{data.frame}, one row per statistic: \code{statistic},
\code{observed}, \code{null_mean}, \code{null_lo}, \code{null_hi}, \code{z}, \code{p_value}, \code{n}.
}
\description{
Tests whether an observed structural statistic exceeds what the degree
sequences alone imply. The null model fixes both margins of the binary
membership (every vertex keeps its hyperdegree, every hyperedge its
size) and randomizes the memberships by checkerboard swaps (Gotelli
2000) -- a sequential MCMC with burn-in \code{10 * nnz} swap attempts and
thinning \code{nnz} between samples, \code{nnz} being the number of memberships.
Statistics are evaluated on the binarized hypergraph (weights carry no
meaning under this null), through the same delegated measures as
\code{\link[=hg_measures]{hg_measures()}}.
}
\details{
The permutation p-value is \code{(1 + extreme) / (n + 1)} (Phipson & Smyth
2010), two-sided by default around the null mean; \code{null_lo}/\code{null_hi}
are the 2.5\% and 97.5\% null quantiles -- report them with the observed
value, not the p-value alone.
}
\section{Conditions}{
 Raises \code{thg_bad_input} for broken contracts.
}

\examples{
hg <- text_hypergraph(c(
  a = "salt and soup and onions",
  b = "soup and salt",
  c = "stars and sky and salt",
  d = "stars and sky"
))
hg_null_test(hg, statistic = "pairwise_participation", n = 49, seed = 1)
}
\references{
Gotelli, N. J. (2000). Null model analysis of species co-occurrence
patterns. \emph{Ecology}, 81(9).

Phipson, B., & Smyth, G. K. (2010). Permutation p-values should never be
zero. \emph{Statistical Applications in Genetics and Molecular Biology}, 9(1).
}
