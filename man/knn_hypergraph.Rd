% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knn.R
\name{knn_hypergraph}
\alias{knn_hypergraph}
\title{Build a k-nearest-neighbor hypergraph from embeddings}
\usage{
knn_hypergraph(embeddings, k, weight = c("cosine", "binary"))
}
\arguments{
\item{embeddings}{Numeric matrix, one row per item, with unique non-empty
rownames (the item IDs). No missing values; no all-zero rows.}

\item{k}{Number of neighbors per hyperedge (between 1 and
\code{nrow(embeddings) - 1}).}

\item{weight}{\code{"cosine"} (default) or \code{"binary"}.}
}
\value{
A \code{net_hypergraph} (from \code{\link[Nestimate:bipartite_groups]{Nestimate::bipartite_groups()}}) with one
hyperedge per item, each of size \code{k + 1}, plus a \code{knn} field recording
\code{k} and the weighting. Accepted by \code{\link[=hg_measures]{hg_measures()}}, \code{\link[=hg_centrality]{hg_centrality()}},
\code{\link[=hg_cluster]{hg_cluster()}}, and \code{\link[=hg_classify]{hg_classify()}}.
}
\description{
Every row of \code{embeddings} becomes one hyperedge containing the row itself
and its \code{k} nearest neighbors by cosine similarity (ties broken
deterministically by row name). With \code{weight = "cosine"} the incidence
entries are the similarities to the hyperedge's center (1 for the center
itself); \code{weight = "binary"} gives the unweighted membership hypergraph
(the \code{HyperG::knn_hypergraph()} construction, which this generalizes).
}
\details{
Cosine similarity and Euclidean distance order neighbors identically on
L2-normalized embeddings, so normalized encoder output (the default of
\code{sbert::encode()}) gives the conventional kNN structure.
}
\section{Conditions}{
 Raises \code{thg_bad_input} for broken contracts, and
\code{thg_nonpositive_similarity} when \code{weight = "cosine"} selects a neighbor
with similarity <= 0 -- a non-positive incidence weight would invalidate
the random-walk machinery downstream; use \code{weight = "binary"} or a
smaller \code{k} instead.
}

\examples{
set.seed(1)
emb <- matrix(rnorm(20, mean = 3), nrow = 5,
              dimnames = list(paste0("doc_", 1:5), NULL))
hg <- knn_hypergraph(emb, k = 2)
hg_measures(hg, what = "edges")
}
